/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint:{
        ignoreDuringBuilds : true,
    },
    javascript:{
        ignoreDuringBuilds : true,
    }
};

export default nextConfig;
